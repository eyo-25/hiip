import styled from "styled-components";
import { Container } from "../Home";

const Interval = () => {
  return (
    <Container>
      <IntervalBox>interval</IntervalBox>
    </Container>
  );
};

export default Interval;

const IntervalBox = styled.div`
  margin-top: 130px;
`;
